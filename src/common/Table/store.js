import {atomWithReset} from 'jotai/utils';

export const TableCheckboxStatusAtom = atomWithReset({});
export const TableDeleteListAtom = atomWithReset([]);

export const dataHasNoIdAtom = atomWithReset(false);
